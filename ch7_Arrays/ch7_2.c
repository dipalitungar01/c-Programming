#include<stdio.h>

int main(){
    // int age = 22;
    // int *ptr =&age;
    // printf("ptr = %u",ptr);
    // ptr++;
    // printf("ptr = %u", ptr);
    // ptr--;
    // printf("ptr = %u", ptr);
    
    //case2
    
   /* float price = 100.00;
    float *ptr =&price;
    printf("ptr = %u",ptr);
    ptr++;
    printf("ptr = %u", ptr);
    ptr--;
    printf("ptr = %u", ptr);*/
    
    char star = '*';
    char *ptr =&star;
    printf("ptr = %u",ptr);
    ptr++;
    printf("ptr = %u", ptr);
    ptr--;
    printf("ptr = %u", ptr);
    return 0;


}